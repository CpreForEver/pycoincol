[project]
name = "pycoincol"
version = "1.0.0"
description = "A Flask application for managing a coin collection"
requires-python = ">=3.9"
dependencies = [
    "flask",
    "flask-wtf>=1.2.2",
    "requests",
]

[tool.uv]
dev-dependencies = []
